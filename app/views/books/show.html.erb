<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'user_info', books: @books %>
      <%= render 'new', book: @book %>
    </div>
    <%= link_to @user.name,  user_path(@user.id) %>
  </div>
</div>
